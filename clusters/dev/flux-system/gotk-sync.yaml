# =============================================================================
# Flux System - GitRepository & Kustomization Sync (Placeholder)
# =============================================================================
# This file is auto-generated by `flux bootstrap github`. It creates:
#
#   1. GitRepository resource: Tells Flux which Git repo to watch and how often
#   2. Kustomization resource: Tells Flux to reconcile the flux-system directory
#
# After running `flux bootstrap github`, this placeholder will be replaced
# with actual manifests containing your repository URL and branch configuration.
#
# Production relevance: The GitRepository resource is the "source of truth"
# connection. Its polling interval (default: 1m) determines how quickly Flux
# detects drift. In production, you might use webhook-based triggering for
# near-instant reconciliation instead of polling.
# =============================================================================

# This placeholder will be replaced by `flux bootstrap github` output.
# See docs/flux-bootstrap.md for step-by-step instructions.
